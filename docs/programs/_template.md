# <ชื่อโปรแกรม>

> สรุปสั้นๆ ว่าโปรแกรมนี้คืออะไร และใช้ทำอะไรในเครื่องนี้

## ติดตั้งจากไหน

- จัดการโดย: `home.nix` / homebrew / installer เอง
- คำสั่ง/ไฟล์ config: `path/to/config`

## ใช้งาน

คำสั่งหรือ workflow ที่ใช้บ่อย

```bash
# ตัวอย่าง
```

## การตั้งค่าที่สำคัญ

อธิบาย option ที่ปรับไว้ และเหตุผล

## ทำไมถึงใช้ตัวนี้

ทางเลือกอื่นที่พิจารณา และเหตุผลที่เลือกตัวนี้ (ถ้ามี)

## ปัญหาที่เจอบ่อย

- อาการ → วิธีแก้
